'use client';

import Calendar from '@/shared/ui/calendar/Calendar';

export default function FaqPage() {
  // 임시로 캘린더 컴포넌트를 확인하기 위한 페이지 작성. 나중에 삭제 및 올바른 곳으로 캘린더 컴포넌트 이동 예정
  return (
    <div className="flex flex-1">
      <Calendar />
    </div>
  );
}
