'use client';

import ScheduleEditorContainer from '@/widgets/schedule/ui/ScheduleEditorContainer';

export default function PostSchedulePage() {
  return <ScheduleEditorContainer entryPoint="post" />;
}
